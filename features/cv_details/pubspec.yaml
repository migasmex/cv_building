name: cv_details
description: Cv details feature
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../../core
  domain:
    path: ../../domain
  core_ui:
    path: ../../core_ui
  navigation:
    path: ../../navigation
  authentication:
    path: ../../features/authentication
  manage_project:
    path: ../../features/manage_project
  project_details:
    path: ../../features/project_details
  cv:
    path: ../../features/cv
  cv_request:
    path: ../../features/cv_request

dev_dependencies:
  flutter_test:
    sdk: flutter

  auto_route_generator: 7.3.2
  build_runner: ^2.4.8